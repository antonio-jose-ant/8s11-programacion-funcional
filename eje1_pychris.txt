def determinar_tipo_dato(entrada):
    try:
        float(entrada)
        return "número flotante"
    except ValueError:
        if entrada.lower() in {"true", "false"}:
            return "booleano"
        return "cadena"

def main():
    entrada = input("Ingrese un dato: ")
    tipo = determinar_tipo_dato(entrada)
    print("El dato ingresado es de tipo:", tipo)

if __name__ == "__main__":
    main()
